public record Sutdent(String id, String name, String dateOfBirth, String classList) {

    //i record non supportano i getter e setter ma hanno incorporato il costruttore con i parametri inseriti tra
    //le parentesi tonde implicitamente
    // i parametri sono final e private
    //immutabile
}
